# User Management API

## Setup
1. Install Node.js
2. Run:
   npm install
   npm start

## API Endpoints

GET /users
GET /users/:id
POST /users
PUT /users/:id
DELETE /users/:id

## Example
POST /users
{
  "name": "John",
  "email": "john@test.com"
}
